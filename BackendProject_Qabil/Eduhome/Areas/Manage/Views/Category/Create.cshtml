@model Category
@{
    ViewData["Title"] = "Create";
}

<div id="page-wrapper">
    <div id="page-inner">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-action">
                        Create Category
                    </div>
                    <div class="card-content">
                        <form class="col s12" method="post">
                            <div class="row">
                                <div class="col s12">
                                    <div class="row m-0">
                                        <div class="input-field col s6">
                                            <input asp-for="Name" class="validate @(Model?.Name!=null?"valid":"")">
                                            <label asp-for="Name" class="@(Model?.Name!=null?"active":"")">Name</label>
                                            <span class="text-danger" asp-validation-for="Name"></span>
                                        </div>
                                        <div class="input-field col s12">
                                            <textarea asp-for="Desc" class="validate @(Model?.Desc!=null?"valid":"")"></textarea>
                                            <label asp-for="Desc" class="@(Model?.Desc!=null?"active":"")">Description</label>
                                            <span class="text-danger" asp-validation-for="Desc"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="waves-effect waves-light btn btn-success" style="width:200px">Create</button>
                        </form>
                        <div class="clearBoth"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

